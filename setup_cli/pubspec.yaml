name: setup_cli
description: A CLI tool to register projects with the Thicket platform and configure IDE integrations.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  path: ^1.9.0
