name: thicket_agent
description: Event-driven autonomous agent runtime for Thicket.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  genkit: any
  genkit_google_genai: any
  http: ^1.6.0
  path: ^1.9.1
  schemantic: any
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  thicket:
    path: ../world_model

dev_dependencies:
  build_runner: any
  test: ^1.24.0
